[project]
name = "simple-latency-agent"
version = "0.1.0"
description = "LiveKit Voice Agent — Speech-to-Speech mit Gemini Live API (Vertex AI Native Audio)"
requires-python = ">=3.11"

dependencies = [
    "livekit-agents>=1.0.0,<2.0.0",
    "livekit-plugins-google>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
